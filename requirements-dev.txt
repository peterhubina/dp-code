# Development and test dependencies. Installed by `make install`, which runs
#
#     pip install -e '.[dev]'
#
# and is the only setup step this repository has. This file mirrors the `dev`
# extra in pyproject.toml so that `pip install -r requirements.txt -r
# requirements-dev.txt` reaches the same environment without the editable
# install — useful only inside the container image, where the source arrives by
# bind mount after the image is built.

-r requirements.txt

# pytest was NOT installed in the environment that produced the published
# numbers, which is why project/CLAM/tests/test_fusion_modes.py is written as a
# plain script and says so in its own docstring. It was added for this test
# suite. Pinned exactly: unpinned it drifts, and the suite asserts on collection
# behaviour.
pytest==8.4.2

# ---------------------------------------------------------------------------
# NOT here, on purpose:
#
#   scikit-survival  a dependency of the DORMANT survival thread
#                    (project/survival/, project/MCAT/utils/) and the one thing
#                    that makes `tools/train_survival.py` and
#                    `tools/eval_survival.py` fail at import with
#                    `No module named 'sksurv'`. It has never been installed in
#                    this environment, so NO version of it ever produced a number
#                    here and any pin would be invented. It is declared, unpinned
#                    and unverified, in the `survival` extra of pyproject.toml:
#
#                        pip install -e '.[survival]'
#
#                    Note that installing it does not make the survival path
#                    work: tools/config/dataset/tcga_brca_survival.yaml still
#                    points `embeddings_dir` at `.datasets/embeddings`, which
#                    does not exist. Two independent breakages.
#
#   ipykernel / jupyter  the old freeze pinned the whole notebook stack as a
#                    runtime dependency. Tracked notebooks are executed by hand,
#                    by whoever already has Jupyter; nothing in the reproduction
#                    path opens one.
#
#   linters          there is no lint configuration in this repository and none
#                    is added here: introducing one would produce a diff across
#                    every tracked file, which is precisely what a refactor that
#                    must not change numerical behaviour cannot afford.
# ---------------------------------------------------------------------------

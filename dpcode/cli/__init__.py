"""Console entry points.

`dp-config` is implemented here. `dp-train`, `dp-evaluate`, `dp-analysis`,
`dp-data` and `dp-cptac` are stubs until the tracks that own them land; each says
so when invoked rather than failing obscurely.
"""

# Configuration reference

Generated by `dp-config reference`. Do not edit by hand.

Values are shown **unresolved**: `${...}` interpolations are what the config
actually contains, and resolving them here would hard-code one machine's
absolute paths into a tracked document. Run `dp-config show` to see the
resolved values for the machine you are on.

## Config groups

| group | options | selected by default |
|---|---|---|
| `acquire` | `cnv`, `embeddings`, `headline_artifacts`, `labels` | `-` |
| `analyses` | `cnv_controls`, `cnv_wsi_fusion`, `compare_fusion_ladder`, `make_cnv_tabular`, `stack_wsi_cnv` | `-` |
| `clam` | `base` | `base` |
| `cptac` | `default` | `-` |
| `evaluate` | `pam50_multimodal` | `-` |
| `experiment` | `er_wsi_alone`, `er_wsi_clinpath_gated`, `er_wsi_rna_film`, `er_wsi_rna_gated`, `pam50_wsi_cnv`, `pam50_wsi_final`, `pam50_wsi_rna_gated` | `-` |
| `fusion` | `coattn`, `concat`, `cross_attention`, `film_attention`, `gated`, `none`, `residual` | `-` |
| `paths` | `default` | `default` |
| `sources` | `default` | `default` |
| `tracking` | `off`, `offline`, `online` | `off` |

Groups added by the training, analysis, evaluation and acquisition entry
points appear once those entry points are installed.

## `paths`

Every filesystem location, resolved from `dpcode/conf/paths/default.yaml`.

`nou_root` is `None` unless `DP_NOU_ROOT` is set: it points at a private
institutional cohort and has no committed default.

| key | value |
|---|---|
| `paths.repo_root` | `'${oc.env:DP_REPO_ROOT,${dp.repo_root:}}'` |
| `paths.data_root` | `'${oc.env:DP_DATA_ROOT,${.repo_root}/.datasets}'` |
| `paths.scratch_root` | `'${oc.env:DP_SCRATCH_ROOT,${.repo_root}/.scratch}'` |
| `paths.results_root` | `'${oc.env:DP_RESULTS_ROOT,${.scratch_root}/results}'` |
| `paths.clam_root` | `'${.repo_root}/project/CLAM'` |
| `paths.splits_root` | `'${.clam_root}/splits'` |
| `paths.dataset_csv_dir` | `'${.clam_root}/dataset_csv'` |
| `paths.labels_dir` | `'${.repo_root}/tools/data'` |
| `paths.tcga_embeddings` | `'${.data_root}/tcga-brca/embeddings'` |
| `paths.cptac_embeddings` | `'${.data_root}/cptac-brca/embeddings'` |
| `paths.cptac_root` | `'${.data_root}/cptac-brca'` |
| `paths.cnv_dir` | `'${.data_root}/cnv'` |
| `paths.cnv_reference_dir` | `'${.cnv_dir}/reference'` |
| `paths.cnv_tabular_dir` | `'${.scratch_root}/cnv-tabular'` |
| `paths.cptac_validation_dir` | `'${.scratch_root}/cptac_validation'` |
| `paths.analysis_dir` | `'${.scratch_root}/analysis'` |
| `paths.harmonisation_dir` | `'${.scratch_root}/harmonisation'` |
| `paths.rna_gdc_dir` | `'${.scratch_root}/rna-gdc'` |
| `paths.rna_legacy_dir` | `'${.scratch_root}/TCGA-BRCA-rna'` |
| `paths.uni_checkpoint_dir` | `'${.scratch_root}/checkpoints/uni2-h'` |
| `paths.hsi_bc_root` | `'${oc.env:DP_HSI_BC_ROOT,${.data_root}/HistologyHSI-BC-Recurrence}'` |
| `paths.nou_root` | `'${oc.env:DP_NOU_ROOT,null}'` |

## `sources`

Remote endpoints and dataset identifiers every acquisition step uses.

Two of these are deliberately visible rather than hidden in code because they
are *not pinned*: the cBioPortal datahub is fetched from a moving branch, and
UCSC `refGene` is a live table. The 39 arm values are therefore not
reproducible from the remotes alone — the cached
`${paths.cnv_reference_dir}/gene_arm_hg38.csv` is what pins the gene->arm map.

| key | value |
|---|---|
| `sources.cbioportal_datahub_base` | `'https://media.githubusercontent.com/media/cBioPortal/datahub'` |
| `sources.cbioportal_datahub_ref` | `'master'` |
| `sources.cbioportal_datahub_url` | `'${.cbioportal_datahub_base}/${.cbioportal_datahub_ref}/public'` |
| `sources.cbioportal_api_base` | `'https://www.cbioportal.org/api'` |
| `sources.tcga_study_id` | `'brca_tcga_pan_can_atlas_2018'` |
| `sources.cptac_study_id` | `'brca_cptac_2020'` |
| `sources.ucsc_goldenpath_base` | `'https://hgdownload.soe.ucsc.edu/goldenPath/hg38/database'` |
| `sources.ucsc_cytoband_file` | `'cytoBand.txt.gz'` |
| `sources.ucsc_refgene_file` | `'refGene.txt.gz'` |
| `sources.gdc_api_base` | `'https://api.gdc.cancer.gov'` |
| `sources.pathdb_base` | `'https://pathdb.cancerimagingarchive.net'` |
| `sources.zenodo_record_id` | `'8394329'` |
| `sources.zenodo_files_url` | `'https://zenodo.org/api/records/${.zenodo_record_id}/files'` |
| `sources.hgnc_complete_set_url` | `'https://storage.googleapis.com/public-download-files/hgnc/tsv/tsv/hgnc_complete_set.txt'` |
| `sources.tcga_cdr_xlsx_url` | `'https://ars.els-cdn.com/content/image/1-s2.0-S0092867418302290-mmc1.xlsx'` |
| `sources.xena_hub_base` | `'https://tcga-xena-hub.s3.us-east-1.amazonaws.com/download'` |
| `sources.hf_features_repo` | `'MahmoodLab/UNI2-h-features'` |
| `sources.hf_encoder_repo` | `'MahmoodLab/UNI2-h'` |

## `clam`

All 52 arguments of `project/CLAM/main.py`, with CLAM's own defaults.

Field order follows the parser's declaration order so this class reads as the
same document as `main.py:120-209` and `dpcode/conf/clam/base.yaml`.

NOTE the arguments that are silently inert on the multimodal path: when
`fusion_mode` is set, `utils/core_utils.py:434` routes to `train_loop` rather
than `train_loop_clam`, so `bag_weight`, `inst_loss` and `no_inst_cluster`
have no effect at all and `B` is passed to the model but never used. The
ladder passes them anyway; they are recorded, not obeyed.

Every field is an argument of `project/CLAM/main.py`, extracted from that
file's own parser. Help text and defaults are CLAM's.

Three of them are NOT composed at the default shown here:
`clam.data_root_dir`, `clam.results_dir`, `clam.split_dir`.
`clam/base.yaml` sets those to interpolations of the `paths` group, so
CLAM's output directory cannot be decoupled from the directory dpcode writes
run metadata into. Run `dp-config show` to see what they compose to.

| key | type | default | choices | help |
|---|---|---|---|---|
| `clam.data_root_dir` | str | `None` |  | data directory |
| `clam.embed_dim` | int | `1024` |  |  |
| `clam.max_epochs` | int | `200` |  | maximum number of epochs to train (default: 200) |
| `clam.lr` | float | `0.0001` |  | learning rate (default: 0.0001) |
| `clam.label_frac` | float | `1.0` |  | fraction of training labels (default: 1.0) |
| `clam.reg` | float | `1e-05` |  | weight decay (default: 1e-5) |
| `clam.seed` | int | `1` |  | random seed for reproducible experiment (default: 1) |
| `clam.k` | int | `10` |  | number of folds (default: 10) |
| `clam.k_start` | int | `-1` |  | start fold (default: -1, last fold) |
| `clam.k_end` | int | `-1` |  | end fold (default: -1, first fold) |
| `clam.results_dir` | str | `'./results'` |  | results directory (default: ./results) |
| `clam.split_dir` | str | `None` |  | manually specify the set of splits to use, instead of infering from the task and label_frac argument (default: None) |
| `clam.log_data` | flag | `False` |  | log data using tensorboard |
| `clam.testing` | flag | `False` |  | debugging tool |
| `clam.early_stopping` | flag | `False` |  | enable early stopping |
| `clam.patience` | int | `10` |  | early stopping patience (default: 10) |
| `clam.opt` | str | `'adam'` | `adam`, `sgd` |  |
| `clam.drop_out` | float | `0.25` |  | dropout |
| `clam.bag_loss` | str | `'ce'` | `svm`, `ce` | slide-level classification loss function (default: ce) |
| `clam.model_type` | str | `'clam_sb'` | `clam_sb`, `clam_mb`, `mil` | type of model (default: clam_sb, clam w/ single attention branch) |
| `clam.exp_code` | str | `None` |  | experiment code for saving results |
| `clam.weighted_sample` | flag | `False` |  | enable weighted sampling |
| `clam.model_size` | str | `'small'` | `small`, `big` | size of model, does not affect mil |
| `clam.task` | str | `None` | `task_1_tumor_vs_normal`, `task_2_tumor_subtyping`, `tcga_brca_recurrence`, `tcga_brca_subtyping`, `tcga_brca_er`, `nou_ctc_ep`, `nou_ctc_emt`, `nou_ctc_any` |  |
| `clam.fusion_mode` | str | `None` | `concat`, `gated`, `residual`, `cross_attention`, `film_attention`, `coattn` | enable WSI + tabular multimodal fusion; supports concat, gated, residual, cross_attention, film_attention and coattn |
| `clam.film_rank` | int | `32` |  | rank of the FiLM conditioner for --fusion_mode film_attention; 0 disables the attention conditioning, leaving additive-logit fusion |
| `clam.modality_dropout` | float | `0.0` |  | probability of dropping the tabular modality during training (film_attention/coattn) |
| `clam.tabular_group_spec` | str | `None` |  | token grouping for --fusion_mode coattn: path to a signature CSV, or "prefix" to group one-hot blocks by column-name prefix |
| `clam.tabular_csv` | str | `None` |  | CSV containing case_id, label and RNA/tabular feature columns |
| `clam.tabular_case_id_col` | str | `'case_id'` |  | case identifier column in --tabular_csv |
| `clam.tabular_label_col` | str | `'label'` |  | label column in --tabular_csv |
| `clam.tabular_hidden_dim` | int | `256` |  | hidden dimension for the tabular MLP encoder |
| `clam.tabular_num_layers` | int | `2` |  | number of hidden layers in the tabular MLP encoder |
| `clam.tabular_top_n_features` | int | `0` |  | select top-N tabular features by training-fold variance; 0 keeps all features |
| `clam.fusion_hidden_dim` | int | `128` |  | hidden dimension for the fusion head; <=0 uses a linear concat head |
| `clam.pretrained_wsi_ckpt` | str | `None` |  | optional WSI-only CLAM checkpoint used to initialize the WSI branch; may include {fold} |
| `clam.freeze_wsi_branch` | flag | `False` |  | freeze the WSI branch after loading --pretrained_wsi_ckpt |
| `clam.pretrained_rna_ckpt` | str | `None` |  | optional matched RNA checkpoint used by residual fusion; may include {fold} |
| `clam.freeze_rna_branch` | flag | `False` |  | freeze the RNA branch after loading --pretrained_rna_ckpt |
| `clam.rna_hidden_dims` | str | `'1024,512'` |  | hidden dimensions for the residual RNA MLP branch |
| `clam.rna_dropout` | float | `0.4` |  | dropout used when constructing the residual RNA MLP branch |
| `clam.residual_scale` | float | `0.2` |  | scale applied to WSI-conditioned residual logits |
| `clam.no_inst_cluster` | flag | `False` |  | disable instance-level clustering |
| `clam.inst_loss` | str | `None` | `svm`, `ce`, `None` | instance-level clustering loss function (default: None) |
| `clam.subtyping` | flag | `False` |  | subtyping problem |
| `clam.bag_weight` | float | `0.7` |  | clam: weight coefficient for bag-level loss (default: 0.7) |
| `clam.B` | int | `8` |  | numbr of positive/negative patches to sample for clam |
| `clam.wandb` | flag | `False` |  | enable wandb logging |
| `clam.wandb_project` | str | `'clam-subtyping'` |  | wandb project name |
| `clam.wandb_entity` | str | `None` |  | wandb entity |
| `clam.wandb_tags` | str | `None` |  | wandb tags (space-separated) |
| `clam.log_heatmaps` | int | `0` |  | log top-K correct + top-K wrong attention heatmaps to W&B per fold (0=off) |

## `fusion`

Which fusion operator a run is, as a label.

The operator's actual CLAM flags (`fusion_mode`, `film_rank`,
`modality_dropout`, `tabular_*`, `fusion_hidden_dim`) live in `clam` and
nowhere else, so there is exactly one source of truth for what is passed to
`main.py`. This node exists so a run directory, a W&B run and a metrics table
can name the ladder arm without re-deriving it from flags.

| key | value |
|---|---|
| `fusion.name` | `'none'` |

## `tracking`

The dpcode-level W&B settings, selected by the `tracking` config group.

The group is `tracking`, not `wandb`, for a mechanical reason: `.gitignore:68`
is a bare `wandb` pattern, which matches a directory of that name at any
depth, so `dpcode/conf/wandb/` could never be committed and a fresh clone
would fail to compose. `.gitignore` is left untouched — anchoring the pattern
would un-ignore `project/CLAM/wandb/`'s 349 committed run directories.

This is NOT the same thing as `clam.wandb*`. CLAM's own W&B flags are part of
its argparse surface and are rendered into argv; this node is what dpcode's
own entry points use, and what an entry point may choose to map onto the
`clam.wandb*` fields. Keeping them separate is deliberate: `clam/base.yaml`
holds CLAM's defaults verbatim, so it cannot quietly change what a legacy
wrapper passed.

| key | value |
|---|---|
| `tracking.enabled` | `False` |
| `tracking.mode` | `'disabled'` |
| `tracking.project` | `'clam-brca-subtyping-cv'` |
| `tracking.entity` | `None` |
| `tracking.tags` | `[]` |
| `tracking.dir` | `'${paths.scratch_root}/wandb'` |

## `run`

Run-level bookkeeping that is not a CLAM flag.

`seed` seeds dpcode's own entry points (the analysis scripts, bootstraps).
It is NOT what CLAM trains with — that is `clam.seed`, kept separate so
`clam/base.yaml` stays a verbatim record of CLAM's defaults. Both are written
into `run_metadata.json`.

| key | value |
|---|---|
| `run.name` | `'unnamed'` |
| `run.seed` | `1` |
| `run.overwrite` | `False` |
| `run.allow_config_surgery` | `False` |

